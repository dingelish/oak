diff '--color=auto' -u -r linux-6.1.84-orig/drivers/virtio/virtio.c linux-6.1.84/drivers/virtio/virtio.c
--- linux-6.1.84-orig/drivers/virtio/virtio.c	2024-04-03 06:19:55.000000000 -0700
+++ linux-6.1.84/drivers/virtio/virtio.c	2024-05-14 09:53:02.366762150 -0700
@@ -177,11 +177,8 @@
 	if (virtio_check_mem_acc_cb(dev)) {
 		if (!virtio_has_feature(dev, VIRTIO_F_VERSION_1)) {
 			dev_warn(&dev->dev,
-				 "device must provide VIRTIO_F_VERSION_1\n");
-			return -ENODEV;
-		}
-
-		if (!virtio_has_feature(dev, VIRTIO_F_ACCESS_PLATFORM)) {
+				"device does not provide VIRTIO_F_VERSION_1 while restricted memory access is enabled!.\n");
+		} else if (!virtio_has_feature(dev, VIRTIO_F_ACCESS_PLATFORM)) {
 			dev_warn(&dev->dev,
 				 "device must provide VIRTIO_F_ACCESS_PLATFORM\n");
 			return -ENODEV;
diff '--color=auto' -u -r linux-6.1.84-orig/drivers/virtio/virtio_pci_modern.c linux-6.1.84/drivers/virtio/virtio_pci_modern.c
--- linux-6.1.84-orig/drivers/virtio/virtio_pci_modern.c	2024-04-03 06:19:55.000000000 -0700
+++ linux-6.1.84/drivers/virtio/virtio_pci_modern.c	2024-05-14 09:58:56.138770545 -0700
@@ -52,9 +52,8 @@
 	vp_transport_features(vdev, features);
 
 	if (!__virtio_test_bit(vdev, VIRTIO_F_VERSION_1)) {
-		dev_err(&vdev->dev, "virtio: device uses modern interface "
+		dev_warn(&vdev->dev, "virtio: device uses modern interface "
 			"but does not have VIRTIO_F_VERSION_1\n");
-		return -EINVAL;
 	}
 
 	vp_modern_set_features(&vp_dev->mdev, vdev->features);
diff '--color=auto' -u -r linux-6.1.84-orig/include/linux/virtio_config.h linux-6.1.84/include/linux/virtio_config.h
--- linux-6.1.84-orig/include/linux/virtio_config.h	2024-04-03 06:19:55.000000000 -0700
+++ linux-6.1.84/include/linux/virtio_config.h	2024-05-14 09:53:50.198763285 -0700
@@ -201,6 +201,9 @@
 	 * Note the reverse polarity of the quirk feature (compared to most
 	 * other features), this is for compatibility with legacy systems.
 	 */
+	if (!virtio_has_feature(vdev, VIRTIO_F_VERSION_1))
+		return false;
+
 	return !virtio_has_feature(vdev, VIRTIO_F_ACCESS_PLATFORM);
 }
 
